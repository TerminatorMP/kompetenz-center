export default function ButtonText({children}) {
    return (
        <button className="text-lg font-medium text-neutral-900 hover:text-neutral-700">
            {children}
        </button>
    )
}

import React from "react";
